express js server template code

const express = require('express')
const app = express()

app.get('/', function (req, res) {
  res.send('Hello World')
})

app.listen(3000)

ejs setup
  1. config ejs = app.set("view engine", "ejs");
  2. config static ejs = app.use("./public")